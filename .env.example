# Stripe server-side credentials. Never expose STRIPE_SECRET_KEY to the browser.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Public, trusted origin used for redirects and absolute metadata URLs.
APP_ORIGIN=http://localhost:3000

# HMAC secret for time-limited booking-management links.
BOOKING_LINK_SECRET=
